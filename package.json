{
  "name": "lawrenceanum",
  "version": "1.0.0",
  "private": true,
  "description": "Root tooling package — wrangler for Cloudflare R2 ops. The SPA lives in figma/.",
  "scripts": {
    "upload-revisions-meta": "wrangler r2 object put \"$R2_BUCKET/revisions_meta.jsonl\" --file=.cache/revisions_meta.jsonl --content-type=application/x-ndjson --remote"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.33.4",
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
