/target
/.cache
/public.tmp
/public.bak
**/*.rs.bk
.DS_Store
# 単発の実行レポート — state/latest.json は commit するが last_run.json は per-run。
/state/last_run.json
# Frontend
**/node_modules
figma/dist
figma/.vite
*.tsbuildinfo
figma/test-results
figma/playwright-report

# wrangler のローカル状態 (R2 ローカルシミュレーション等)
/.wrangler

# 生成物 public/ は git に載せない (履歴肥大を避ける)。CI が lawpub + vite で
# 再生成し、gzip 圧縮して Pages artifact 経由で deploy する (search.db は R2)。
/public

